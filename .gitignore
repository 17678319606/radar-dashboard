# 本地密钥 / 虚拟环境（含真实 DeepSeek Key 的 .env 切勿提交）
.env
venv/
.venv/
__pycache__/
*.pyc
dist/.env

# 构建产物：由 build_static.py 每次重新生成，不入库
# （workflow 会把 dist/radar-api/ 复制到仓库根 radar-api/ 再提交）
dist/
.edgeone/

# 本地 Flask 运行产物
*.log

# 注意：data/ 与 radar-api/ 必须可提交，是前端每日数据来源，切勿忽略
